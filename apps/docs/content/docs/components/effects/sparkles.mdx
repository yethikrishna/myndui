---
title: Sparkles
description: A canvas field of twinkling particles rendered behind its content.
---

import { Sparkles } from "@godui/components";
import { SparklesDemo } from "@/components/demos/sparkles-demo";

<ComponentPreview
  code={`import { Sparkles } from "@/components/godui/sparkles";

export function SparklesDemo() {
  return (
    <Sparkles className="flex h-52 w-full items-center justify-center rounded-2xl bg-card">
      <h2 className="text-4xl font-semibold text-foreground">Magic</h2>
    </Sparkles>
  );
}`}
>
  <SparklesDemo />
</ComponentPreview>

## Installation

<ComponentInstall componentName="Sparkles" />

Particles are seeded from the canvas area and twinkle by oscillating their
opacity on a single `requestAnimationFrame` loop, resizing with a
`ResizeObserver`. The color string is resolved against the live theme, so
`var(--primary)` tracks light and dark. Under `prefers-reduced-motion` the field
renders a single static frame.

## Usage

```tsx
import { Sparkles } from "@/components/godui/sparkles";
```

```tsx
<Sparkles className="flex h-52 items-center justify-center">
  <h2>Magic</h2>
</Sparkles>
```

### Denser, colored field

<ComponentPreview
  code={`<Sparkles density={70} color="oklch(0.8 0.13 90)" className="flex h-52 w-full items-center justify-center rounded-2xl bg-card">
  <h2 className="text-4xl font-semibold text-foreground">Gold</h2>
</Sparkles>`}
>
  <Sparkles density={70} color="oklch(0.8 0.13 90)" className="flex h-52 w-full items-center justify-center rounded-2xl bg-card">
    <h2 className="text-4xl font-semibold text-foreground">Gold</h2>
  </Sparkles>
</ComponentPreview>

## Props

| Prop      | Type     | Default          | Description                                    |
| --------- | -------- | ---------------- | ---------------------------------------------- |
| `color`   | `string` | `var(--primary)` | Particle color.                                |
| `density` | `number` | `30`             | Particles per 10,000 px² of canvas area.       |
| `minSize` | `number` | `0.6`            | Minimum particle radius in pixels.             |
| `maxSize` | `number` | `1.6`            | Maximum particle radius in pixels.             |
| `speed`   | `number` | `1`              | Twinkle speed multiplier.                      |

`Sparkles` also forwards every standard `<div>` attribute to the root element.
