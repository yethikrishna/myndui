---
title: Switch
description: An Apple-grade toggle with a spring thumb you can drag, and a track that crossfades.
---

import { Switch } from "@godui/components";

<ComponentPreview
  code={`import { Switch } from "@/components/godui/switch";

export function SwitchDemo() {
  return <Switch defaultChecked />;
}`}
>
  <Switch defaultChecked />
</ComponentPreview>

## Sizes

<ComponentPreview
  code={`import { Switch } from "@/components/godui/switch";

export function SwitchSizes() {
  return (
    <div className="flex items-center gap-6">
      <Switch size="sm" defaultChecked />
      <Switch size="md" defaultChecked />
      <Switch size="lg" defaultChecked />
    </div>
  );
}`}
>
  <div className="flex items-center gap-6">
    <Switch size="sm" defaultChecked />
    <Switch size="md" defaultChecked />
    <Switch size="lg" defaultChecked />
  </div>
</ComponentPreview>

## Installation

<ComponentInstall name="switch" />

## Usage

```tsx
import { Switch } from "@godui/components";

const [on, setOn] = useState(false);

<Switch checked={on} onCheckedChange={setOn} />;
```

You can click the track or drag the thumb across it.

## Props

| Prop | Type | Default | Description |
| ---- | ---- | ------- | ----------- |
| `checked` | `boolean` | — | Controlled state. |
| `defaultChecked` | `boolean` | `false` | Uncontrolled state. |
| `size` | `"sm" \| "md" \| "lg"` | `"md"` | Switch size. |
| `disabled` | `boolean` | `false` | Disable interaction. |
| `onCheckedChange` | `(checked: boolean) => void` | — | Fires on change. |
