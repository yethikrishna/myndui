{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "scroll-progress",
  "title": "Scroll Progress",
  "description": "A spring-smoothed reading indicator — a pinned top bar or a fading back-to-top ring.",
  "dependencies": ["framer-motion"],
  "registryDependencies": ["@godui/godui-theme"],
  "files": [
    {
      "path": "packages/components/src/scroll-progress/scroll-progress.tsx",
      "content": "\"use client\";\n\nimport {\n  AnimatePresence,\n  motion,\n  useReducedMotion,\n  useScroll,\n  useSpring,\n  useTransform,\n} from \"framer-motion\";\nimport * as React from \"react\";\n\nexport type ScrollProgressProps = React.HTMLAttributes<HTMLDivElement> & {\n  /** `bar` pins a progress line; `circle` shows a fading back-to-top ring. */\n  variant?: \"bar\" | \"circle\";\n  /** Track scroll within a specific element instead of the whole page. */\n  target?: React.RefObject<HTMLElement>;\n  /** Bar thickness in px (bar variant). */\n  height?: number;\n  /** Ring diameter in px (circle variant). */\n  size?: number;\n  /** Reveal the circle only after this progress fraction (0–1). */\n  showAfter?: number;\n};\n\nconst SPRING = { stiffness: 120, damping: 24, restDelta: 0.001 };\n\nconst ScrollProgress = React.forwardRef<HTMLDivElement, ScrollProgressProps>(\n  (\n    {\n      variant = \"bar\",\n      target,\n      height = 3,\n      size = 44,\n      showAfter = 0.05,\n      className,\n      ...props\n    },\n    ref,\n  ) => {\n    const reduceMotion = useReducedMotion();\n    const { scrollYProgress } = useScroll(\n      target ? { target, offset: [\"start start\", \"end end\"] } : undefined,\n    );\n    const progress = useSpring(\n      scrollYProgress,\n      reduceMotion ? { stiffness: 1000, damping: 100 } : SPRING,\n    );\n\n    if (variant === \"circle\") {\n      return (\n        <CircleProgress\n          ref={ref}\n          progress={progress}\n          rawProgress={scrollYProgress}\n          size={size}\n          showAfter={showAfter}\n          className={className}\n          {...props}\n        />\n      );\n    }\n\n    return (\n      <motion.div\n        ref={ref}\n        role=\"progressbar\"\n        aria-label=\"Page scroll progress\"\n        style={{ scaleX: progress, height }}\n        className={`fixed inset-x-0 top-0 z-sticky origin-left bg-primary ${\n          className ?? \"\"\n        }`}\n        {...(props as React.ComponentProps<typeof motion.div>)}\n      />\n    );\n  },\n);\nScrollProgress.displayName = \"ScrollProgress\";\n\ntype CircleProps = ScrollProgressProps & {\n  progress: ReturnType<typeof useSpring>;\n  rawProgress: ReturnType<typeof useScroll>[\"scrollYProgress\"];\n};\n\nconst CircleProgress = React.forwardRef<HTMLDivElement, CircleProps>(\n  ({ progress, rawProgress, size = 44, showAfter = 0.05, className }, ref) => {\n    const [visible, setVisible] = React.useState(false);\n    React.useEffect(() => {\n      const unsub = rawProgress.on(\"change\", (v) =>\n        setVisible(v > (showAfter ?? 0.05)),\n      );\n      return unsub;\n    }, [rawProgress, showAfter]);\n\n    const r = (size - 6) / 2;\n    const circumference = 2 * Math.PI * r;\n    const dashoffset = useTransform(\n      progress,\n      (v) => circumference * (1 - Math.min(Math.max(v, 0), 1)),\n    );\n\n    return (\n      <AnimatePresence>\n        {visible && (\n          <motion.button\n            ref={ref as React.Ref<HTMLButtonElement>}\n            type=\"button\"\n            aria-label=\"Back to top\"\n            onClick={() =>\n              window.scrollTo({ top: 0, behavior: \"smooth\" as ScrollBehavior })\n            }\n            initial={{ opacity: 0, scale: 0.7 }}\n            animate={{ opacity: 1, scale: 1 }}\n            exit={{ opacity: 0, scale: 0.7 }}\n            transition={{ type: \"spring\", stiffness: 400, damping: 28 }}\n            style={{ width: size, height: size }}\n            className={`fixed bottom-6 right-6 z-sticky grid place-items-center rounded-full border border-border bg-background text-foreground shadow-lg ${\n              className ?? \"\"\n            }`}\n          >\n            <svg\n              width={size}\n              height={size}\n              viewBox={`0 0 ${size} ${size}`}\n              className=\"absolute inset-0 -rotate-90\"\n              aria-hidden=\"true\"\n            >\n              <circle\n                cx={size / 2}\n                cy={size / 2}\n                r={r}\n                fill=\"none\"\n                stroke=\"currentColor\"\n                strokeWidth={3}\n                className=\"text-border\"\n              />\n              <motion.circle\n                cx={size / 2}\n                cy={size / 2}\n                r={r}\n                fill=\"none\"\n                stroke=\"currentColor\"\n                strokeWidth={3}\n                strokeLinecap=\"round\"\n                strokeDasharray={circumference}\n                style={{ strokeDashoffset: dashoffset }}\n                className=\"text-primary\"\n              />\n            </svg>\n            <svg\n              viewBox=\"0 0 24 24\"\n              fill=\"none\"\n              stroke=\"currentColor\"\n              strokeWidth={2}\n              strokeLinecap=\"round\"\n              strokeLinejoin=\"round\"\n              className=\"size-4\"\n              aria-hidden=\"true\"\n            >\n              <path d=\"m18 15-6-6-6 6\" />\n            </svg>\n          </motion.button>\n        )}\n      </AnimatePresence>\n    );\n  },\n);\nCircleProgress.displayName = \"CircleProgress\";\n\nexport { ScrollProgress };\n",
      "type": "registry:ui",
      "target": "components/godui/scroll-progress.tsx"
    }
  ],
  "type": "registry:ui"
}
